language: julia

os:
  - linux
  - osx

julia:
  - 0.5
  - 0.6
  - nightly

notifications:
  email: false

script:
  - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
  - julia --check-bounds=yes --color=yes -e 'Pkg.clone(pwd()); Pkg.build("GeoStatsImages"); Pkg.test("GeoStatsImages"; coverage=true)'

after_success:
  - julia -e 'cd(Pkg.dir("GeoStatsImages")); Pkg.add("Coverage"); using Coverage; Codecov.submit(process_folder())'
